# 0.1.2 起 dsh 把大量运行时依赖改写成 peer（降低解析成本），靠安装器自动装 peer 生效。
# 本仓库开 legacy-peer-deps 显式钉版本，因此宿主内部基础包（dsh-jobs、dsh-session-persistence、
# dsh-session-query、dsh-util-time、dsh-util-workspace-path、dsh-hook-protocol、dsh-sdk-protocol）
# 必须显式列进 devDependencies 才会安装；升级 dsh 后需重扫宿主包的非可选 peer 补齐。
legacy-peer-deps=true
